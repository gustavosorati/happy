import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    zIndex: 10,
    width: "100%",
    height: "100%",
    position: 'absolute',
    top: 0,
    left: 0,
  },
  gradient: {
    flex: 1,
    width: "100%",
    height: "100%",
    alignItems: 'center',
    justifyContent: 'center',
  }
})
